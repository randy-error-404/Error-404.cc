# Man page data

The browser lab uses indexed reference text. The production project can expand this into a full searchable man-page dataset with source/licensing attribution for the selected Enterprise Linux release.
