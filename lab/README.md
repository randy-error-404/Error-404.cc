# Error404 Linux Lab

Browser-native Enterprise Linux administration simulator for error-404.cc.

The lab keeps a stateful virtual Linux system in the browser: filesystem, users/groups, permissions, services, logs, networking, storage, packages, firewall and SELinux state. Commands operate on that state rather than returning canned output.

No Codespaces, public shell, or host access is required.
